<header>
	<div class="logo_ctn">
		<a href="{{path('app_index')}}">
			<img class="logo_img" src="{{ asset('images/logo.png') }}" alt="logo du site">
		</a>
	</div>
	<div class="flash_ctn">
		{% include "partials/_flash.html.twig" %}
	</div>

	<div class="log_action_ctn">
		{% if app.user %}
			<a href="{{path('app_logout')}}">SE DECONNECTER
				<i class="fas fa-user-times"></i>
			</a>
		{% else %}
			<a href="{{path('app_login')}}">SE CONNECTER
				<i class="fas fa-user"></i>
			</a>
		{% endif %}
	</div>
</header>
